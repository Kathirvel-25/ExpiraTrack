npm init -y
npm install express cors mysql2 jsonwebtoken bcrypt multer sharp dotenv
npm install --save-dev nodemon


these are the additionl pakage installed 


for the backend i use the Mysql for....the database is expiratrack
